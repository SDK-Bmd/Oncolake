"""Stage DVC : ingest. Remplit la zone raw (MinIO) + ecrit data/raw/manifest.json."""
# TODO etape 3 : appeler ingest.uniprot.search_by_keyword + ingest.alphafold.fetch_cif,
#                pousser dans MinIO (storage.put_object), ecrire le manifest.
if __name__ == "__main__":
    raise SystemExit("stage_ingest : a implementer (etape 3)")
